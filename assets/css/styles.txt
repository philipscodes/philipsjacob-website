body {
  padding-top: 70px; /* Adjusted to ensure content below the header is visible */
  background-color: #f8f9fa; /* Light grey background */
}


header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  background-color: #007bff; /* Your existing blue background */
  color: white;
  padding: 5px 20px; /* Reduced padding for a slimmer header */
  margin: 10px 20px; /* Keeps margin to maintain the container-like feel */
  border-radius: 10px;
  box-shadow: 0 2px 5px rgba(0,0,0,0.2);
  z-index: 1000;
  transition: all 0.5s ease; /* Smooth transition for any size changes */
  box-sizing: border-box;
  height: 80px; /* Reduced initial height */
  display: flex;
  align-items: center; /* Vertically centers the content */
  justify-content: space-between; /* Spaces out the elements */
}

header.small {
  padding: 5px 15px; /* Even smaller padding when scrolled */
  height: 60px; /* Further reduced height when scrolled */
  margin-top: 5px; /* Reduced top margin when scrolled */
  margin-bottom: 5px; /* Reduced bottom margin when scrolled */
  font-size: 20px; /* Slightly smaller font size when header is small */
}


.header-title {
  flex-grow: 1; /* Allows the title to take up the remaining space */
  text-align: center; /* Centers the title text */
  font-size: 24px; /* Larger font size */
}

header img {
  height: 100%;  /* Makes the image height always equal to the header height */
  width: auto;   /* Maintains the aspect ratio of the image */
  transition: height 0.5s ease-in-out;  /* Smooth transition of image resizing */
}

header.small img {
  height: 100%;  /* Adjusts automatically with header */
}





.header-profile-pic {
  width: 100px; /* Initial width of the image */
  transition: width 0.5s; /* Smooth transition for resizing image */
}

/* Smaller image on scroll */
.scrolled .header-profile-pic {
  width: 50px; /* Smaller width when page is scrolled */
}

.navbar {
  width: 100%; /* Ensures the navbar spans the full width of the header */
}

.body, .content {
  padding-top: 180px;  // Adjust based on the actual height of your header
}


.tiles-container {
  display: flex;
  flex-direction: column;
}

.intro-container {
  background-color: #f8f9fa; /* Light background color for visibility */
  border-radius: 10px; /* Rounded corners */
  padding: 20px; /* Padding inside the container */
  margin: 20px auto; /* Margin to center it and give space around */
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Optional: adds shadow for depth */
  max-width: 1200px; /* Maximum width of the container */
}


.tiles-container .card {
  flex: 1;
}

.content {
  padding-top: 100px; /* Adjust according to the final size of the header */
}

@media (max-width: 768px) {
  header {
      padding: 5px 10px;
      height: 60px;
  }
  header.small {
      height: 50px;
  }
  .content {
      padding-top: 70px;
  }
}
